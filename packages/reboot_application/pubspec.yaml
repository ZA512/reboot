name: reboot_application
description: Commands, use cases, and ports for REBOOT.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  reboot_domain: ^0.1.0
  reboot_projection: ^0.1.0

dev_dependencies:
  test: 1.31.0
